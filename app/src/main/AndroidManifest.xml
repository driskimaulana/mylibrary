<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mylibrary">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyLibrary">
        <activity android:name=".WebViewActivity"></activity>
        <activity android:name=".FavoriteBooksActivity"
            android:label="@string/activity_favoriteBook"/>
        <activity android:name=".WantToReadBooksActivity"
            android:label="@string/activity_wantToRead"/>
        <activity android:name=".CurrentlyReadingBooksActivity"
            android:label="@string/activity_currentRead"/>
        <activity android:name=".AlreadyReadBooksActivity"
            android:label="@string/activity_alreadyRead"/>
        <activity android:name=".BookActivity" />
        <activity android:name=".AllBooksActivity"
            android:label="@string/activity_allBook"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>